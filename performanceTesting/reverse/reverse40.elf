%query 1 *  reverse (cons a (cons a (cons a (cons a (cons a
                       (cons a (cons a (cons a (cons a (cons a 
                       (cons a (cons a (cons a (cons a (cons a
                       (cons a (cons a (cons a (cons a (cons a 
                       (cons a (cons a (cons a (cons a (cons a
                       (cons a (cons a (cons a (cons a (cons a 
                       (cons a (cons a (cons a (cons a (cons a
                       (cons a (cons a (cons a (cons a (cons a 
                       nil)))))))))))))))))))))))))))))))))))))))) L.
